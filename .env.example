# ─────────────────────────────────────────────────────────────
# App
# ─────────────────────────────────────────────────────────────

# Host ports. Change these if something already listens there, the containers
# keep talking to each other on 3000, 6333 and 8000 regardless.
BOT_PORT=8000
WAHA_PORT=3000
QDRANT_PORT=6333

# Name the bot uses when introducing itself.
BUSINESS_NAME=Innovacar

# Language the bot always replies in.
BOT_LANGUAGE=español


# ─────────────────────────────────────────────────────────────
# WAHA  (WhatsApp HTTP API)
# ─────────────────────────────────────────────────────────────

# Reachable from THIS process, not from inside the waha container.
#   app on host, waha in docker  -> http://localhost:3000
#   both in docker compose       -> http://waha:3000
WAHA_URL=http://localhost:3000

# Must match WHATSAPP_API_KEY on the waha container. Empty disables auth.
WAHA_API_KEY=

# Session name. Waha Core only supports "default".
WAHA_SESSION=default

# Shared secret for webhook signatures. Must match WHATSAPP_HOOK_HMAC_KEY
# on the waha container. Leave empty ONLY for local testing.
WEBHOOK_SECRET=

# Seconds to wait for more messages before answering, so three quick
# messages become one reply instead of three.
DEBOUNCE_SECONDS=6


# ─────────────────────────────────────────────────────────────
# OpenAI
# ─────────────────────────────────────────────────────────────

OPENAI_API_KEY=

# Chat model. Needs vision support to read images.
OPENAI_MODEL=gpt-4o

# Embedding model. Changing it requires re-ingesting every collection.
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Speech to text model for voice notes.
OPENAI_TRANSCRIPTION_MODEL=whisper-1

# 0.0 is deterministic, 1.0 is creative.
OPENAI_TEMPERATURE=0.3


# ─────────────────────────────────────────────────────────────
# Qdrant  (vector database)
# ─────────────────────────────────────────────────────────────

#   app on host, qdrant in docker -> http://localhost:6333
#   both in docker compose        -> http://qdrant:6333
QDRANT_URL=http://localhost:6333

# Only needed for Qdrant Cloud.
QDRANT_API_KEY=

# Knowledge base ingested from your own documents.
QDRANT_KNOWLEDGE_COLLECTION=knowledge

# Conversation memory, one point per message, filtered by chat id.
QDRANT_MEMORY_COLLECTION=memory

# Sparse model for the keyword half of hybrid search. Runs locally on cpu.
SPARSE_EMBEDDING_MODEL=Qdrant/bm25


# ─────────────────────────────────────────────────────────────
# Retrieval tuning
# ─────────────────────────────────────────────────────────────

# Documents pulled from the knowledge base per question.
KNOWLEDGE_TOP_K=6

# Past messages semantically recalled for this user.
MEMORY_TOP_K=6

# Most recent messages always included, regardless of relevance.
MEMORY_RECENT_TURNS=10


# ─────────────────────────────────────────────────────────────
# WAHA container only
# Read by docker compose, not by the app. Ignored when running locally.
# ─────────────────────────────────────────────────────────────

# Protects the WAHA dashboard at http://localhost:3000/dashboard
WAHA_DASHBOARD_USERNAME=waha
WAHA_DASHBOARD_PASSWORD=

# Protects the swagger docs at http://localhost:3000
WHATSAPP_SWAGGER_USERNAME=waha
WHATSAPP_SWAGGER_PASSWORD=
